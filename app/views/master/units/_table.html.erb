<div class="table-responsive">
  <table class="table">
    <thead>
      <tr>
        <% enabled_grid_option_keys.each do |key| %>
          <%= generate_table_header(key) %>
        <% end %>
        <th scope="col">Actions</th>
      </tr>
    </thead>

    <tbody>
      <% @units.each do |unit| %>
        <tr>
          <% enabled_grid_option_keys.each do |key| %>
            <td><%= units_table_defination(key, unit) %></td>
          <% end %>
          <td>
            <div class="button-group">
              <% if unit.deleted? %>
                <%= restore_link(controller_name, unit) %>
              <% else %>
                <%= edit_link_using_modal(controller_name, unit) %>
                <%= archive_link(controller_name, unit) if unit.active? %>
                <%= unarchive_link(controller_name, unit) if unit.archived? %>
                <%= delete_link(controller_name, unit) %>
              <% end %>
            </div>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
