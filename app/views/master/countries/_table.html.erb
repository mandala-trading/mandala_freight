<div class="table-responsive">
  <table class="table">
    <thead>
      <tr>
        <% enabled_grid_option_keys.each do |key| %>
          <%= generate_table_header(key) %>
        <% end %>
        <th scope="col">Actions</th>
      </tr>
    </thead>

    <tbody>
      <% @countries.each do |country| %>
        <tr>
          <% enabled_grid_option_keys.each do |key| %>
            <td><%= countries_table_defination(key, country) %></td>
          <% end %>
          <td>
            <div class="button-group">
              <%= edit_link_using_modal(controller_name, country) %>
              <%= delete_link(controller_name, country) if country.undiscarded? %>
              <%= restore_link(controller_name, country) if country.discarded? %>
            </div>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
