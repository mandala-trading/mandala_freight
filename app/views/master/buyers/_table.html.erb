<div class="table-responsive">
  <table class="table">
    <thead>
      <tr>
        <% enabled_grid_option_keys.each do |key| %>
          <%= generate_table_header(key) %>
        <% end %>
        <th scope="col">Actions</th>
      </tr>
    </thead>

    <tbody>
      <% @buyers.each do |buyer| %>
        <tr>
          <% enabled_grid_option_keys.each do |key| %>
            <td><%= buyers_table_defination(key, buyer) %></td>
          <% end %>
          <td>
            <div class="button-group">
              <% if buyer.deleted? %>
                <%= restore_link(controller_name, buyer) %>
              <% else %>
                <%= edit_link_using_modal(controller_name, buyer) %>
                <%= archive_link(controller_name, buyer) if buyer.active? %>
                <%= unarchive_link(controller_name, buyer) if buyer.archived? %>
                <%= delete_link(controller_name, buyer) %>
              <% end %>
            </div>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
