<div class="table-responsive">
  <table class="table">
    <thead>
      <tr>
        <% enabled_grid_option_keys.each do |key| %>
          <%= generate_table_header(key) %>
        <% end %>
        <th scope="col">Actions</th>
      </tr>
    </thead>

    <tbody>
      <% @payment_types.each do |payment_type| %>
        <tr>
          <% enabled_grid_option_keys.each do |key| %>
            <td><%= payment_types_table_defination(key, payment_type) %></td>
          <% end %>
          <td>
            <div class="button-group">
              <% if payment_type.deleted? %>
                <%= restore_link(controller_name, payment_type) %>
              <% else %>
                <%= edit_link_using_modal(controller_name, payment_type) %>
                <%= archive_link(controller_name, payment_type) if payment_type.active? %>
                <%= unarchive_link(controller_name, payment_type) if payment_type.archived? %>
                <%= delete_link(controller_name, payment_type) %>
              <% end %>
            </div>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
