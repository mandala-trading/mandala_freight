$('#form_modal').find('.modal-content').html('<%= j (render "shared/quick_filters") %>');
$('#form_modal').modal('show');

<% if defined?(@filter_option) && @filter_option.present? %>
  $('#filter_<%= @filter_option.id %>').remove();
<% end %>
